
#include <math.h>

double stdDeviation(double* value, int numValues); // Calculate std. deviation of the mean

float pu2(float value, float uncertainty); // Calculate the percent uncertainty squared of a value.
